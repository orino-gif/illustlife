%table.table
  %tr
    - client = '依頼者：' + @tx.nick
    %td= icon(@tx.cre,'s_square') + client
  %tr
    - auth = '承認者：' + @rx.nick
    %td= icon(@rx.cre,'s_square') + auth
  %tr
    %td.m_line= 'リクエスト内容：' + @req.msg.to_s
  %tr
    %td= "ファイル形式：#{@req.fmt}"
    - if @req.anon?
      %tr
        %td= '非公開:有'
    - if @req.auto?
      %tr
        %td= '宛名入りサイン:有'
  - if user_signed_in?
    - if @tx.id == current_user.id || @rx.id == current_user.id
      - relation = true
    - if relation || Rails.env.development?
      %tr
        %td= "金額：#{@req.money}円"
      %tr
        - num_days = @rx.cre.sttg.work_d * 60 * 60 * 24
        - dl = @req.created_at + num_days
        %td.m_line
          = "締め切り日：#{dl.strftime('%Y/%m/%d %H:%M')}"
      %tr
        %td= "状態：#{@req.stts}"