/ CSRFとコンテンツセキュリティポリシー対策
= csrf_meta_tags
= csp_meta_tag

/ google検索に表示しない
- if 'agreements' == controller.controller_name || 'index' == controller.action_name || 'new' == controller.action_name || 'edit' == controller.action_name 
  %meta{:content => "noindex", :name => "robots"}/