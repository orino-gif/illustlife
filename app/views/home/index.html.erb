<h1>クリエイター紹介</h1><br>
<div class="container-fluid">
  <div class="row">
    <h3>・ディフォルメキャラ(2～3頭身)</h3>
  </div>
  
  <div class="row">
    <% begin %>
      <%# raise #意図的にエラーを発生%>
      <% @creators.each do |creator| %>
        <div class="col-md-2">
          <% if creator.user.confirmed_at? %>
            <% if creator.icon? %>
              <%= link_to image_tag(creator.icon.url  ,class: 'icon center-block'),  "/home/show/#{creator.user_id}" %>
            <% else %>
              <%= link_to image_tag('/mobu.png',class: 'icon'), "/home/show/#{creator.user_id}" %>
            <% end %>
            <div class="text-center">
              <span style="font-size: 18px;">
                <%= creator.user.nickname %>
              </span>
            </div>
          <% end %>
        </div>
      <% end %>
      
    <% rescue => e %>
        <%=p e%>
    <% end %>
  </div>

  <h3>背景</h3><br>
  
  
  <h3>前景(道具、衣装、エフェクト等)</h3><br>
  
  <h3>キャラバストアップ(4頭身以上)</h3><br>
  
  <h3>スライドショー</h3><br>
</div>

<%= link_to "test1", new_creator_path, method: "get" %>
<%= link_to "test2", creators_path, method: "get" %>

