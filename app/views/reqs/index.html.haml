- content_for(:html_title) { 'リクエスト一覧' }
%h1.ll_font.ws_bgc リクエスト一覧
.container
  / 表題
  .row
    %table.table.fixed_col
      %tr
        %th="申請日時"
        %th="依頼者"
        %th="承認者"
        %th="状態"
        %th=""
  
  / 表体
  - if @reqs
    - @reqs.reverse_each.with_index(1) do |req|
      - @req = req
      = render 'req.html'
  - else
    = 'リクエストがまだ存在しません'
