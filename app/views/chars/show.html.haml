- title="キャラデータ：#{@char.c_name} #{@char.ttl.t_name} 」"
- content_for(:html_title) { title }
%h1.ll_font.ws_bgc
  = title
.container
  .row
    %h2.l_font= '好感度グラフ'
  .row.my-1
    - if @path.img?
      = image_tag(@path.img.url,
      class: 'll_square full_rect fit_c border rounded-circle')
  .row
    %h2.l_font= '引用先'

