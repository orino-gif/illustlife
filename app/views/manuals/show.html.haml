- content_for(:html_title) { 'イラスト付き利用説明' }
%h1.font-Xlarge.background_color-whitesmoke='イラスト付き利用説明'

.container
  - case params[:id]
  - when '1'
    = render 'manuals/parts/test'
  - when '2'
    = render 'manuals/parts/charadeza'